{
  "threshold": {
    "type": "scale",
    "default": 15,
    "min": 1,
    "max": 50,
    "step": 1,
    "units": "%",
    "description": "Threshold percentage"
  },
  "action": {
    "type": "combobox",
    "default": "alert",
    "description": "Action when threshold reached",
    "options": {
      "Suspend": "suspend",
      "Hibernate": "hibernate",
      "Alert only": "alert"
    }
  },
  "check_interval": {
    "type": "spinbutton",
    "default": 30,
    "min": 5,
    "max": 300,
    "step": 1,
    "units": "sec",
    "description": "Check interval (seconds)"
  },
  "cooldown_minutes": {
    "type": "spinbutton",
    "default": 10,
    "min": 1,
    "max": 120,
    "step": 1,
    "units": "min",
    "description": "Cooldown after action (minutes)"
  },
  "notify": {
    "type": "checkbox",
    "default": true,
    "description": "Show notification before action"
  },
  "popup_alert": {
    "type": "checkbox",
    "default": false,
    "description": "Show popup alert before action"
  },
  "popup_seconds": {
    "type": "spinbutton",
    "default": 20,
    "min": 5,
    "max": 120,
    "step": 1,
    "units": "sec",
    "description": "Popup countdown seconds"
  },
  "only_discharging": {
    "type": "checkbox",
    "default": true,
    "description": "Only act while discharging"
  },
  "force_critical": {
    "type": "checkbox",
    "default": true,
    "description": "Force sleep/hibernate at critical threshold when action is Alert only"
  },
  "critical_threshold": {
    "type": "scale",
    "default": 5,
    "min": 3,
    "max": 10,
    "step": 1,
    "units": "%",
    "description": "Critical threshold percentage"
  },
  "critical_action": {
    "type": "combobox",
    "default": "suspend",
    "description": "Critical action (used only when action is Alert only)",
    "options": {
      "Suspend": "suspend",
      "Hibernate": "hibernate"
    }
  },
  "fix_deep_sleep": {
    "type": "button",
    "description": "Install suspend/resume fix",
    "callback": "on_fix_deep_sleep",
    "tooltip": "Runs suspend-fix/apply.sh to install suspend/resume hooks (requires admin password)."
  }
}
